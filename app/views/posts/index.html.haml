.posts-wrapper.row
	-@posts.each do |post|
		.post
			.post-head
				.name
					Kong X
			.image.center-block
				=link_to (image_tag post.image.url, class: 'img-responsive'), post_path(post)
				%p.caption
					=post.caption